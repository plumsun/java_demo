build模式
通过抽象建造者构建对象流程，使同样的流程可以创建不同的表示。
好处：
    1、将一个复杂对象的创建过程封装起来。
    2、解耦了组装过程和创建具体部件，使得我们不用去关心每个部件是如何组装的。
    3、向客户隐藏产品内部的表现。
    4、产品的实现可以被替换，因为客户只看到一个抽象的接口。
build：具体的构建者（建筑工人） Director：导演（监工）
通过director组装build抽象的建筑流程，并对外暴露API.